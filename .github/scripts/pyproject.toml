[project]
name = "github-scripts"
version = "0.1.0"
description = "Helper scripts run by .github/workflows/ci.yml, and their tests"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3",
]

[tool.uv]
package = false
